{
  "homepage": "https://AnukratiMehta.github.io/bar-chart",
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },

  "scripts":{
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}
